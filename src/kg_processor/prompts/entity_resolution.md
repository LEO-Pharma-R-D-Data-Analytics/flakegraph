Decide whether pairs of grounded mentions refer to the same real-world entity.
Merge only when names, types, aliases, descriptions, and evidence show identity.
Similarity or association is not identity.
A family and its members, sibling versions or sizes, and constrained and generic
methods are not identical unless the source explicitly makes them aliases.
Retain identity-bearing qualifiers such as version, scale, guidance, domain, and
constraint labels; never move one sibling variant into another variant's aliases.
Keep different arts, branches, organizations, people, places, events, and
periods distinct.
Return one decision for every supplied pair and preserve a concise reason.
